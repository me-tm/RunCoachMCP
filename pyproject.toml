[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "claude2strava"
version = "0.1.0"
description = "RunCoach MCP — Claude MCP server for Garmin Connect training analysis"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "cryptography>=43.0.0",
    "pydantic-settings>=2.6.0",
    "python-multipart>=0.0.12",
    "jinja2>=3.1.0",
    "itsdangerous>=2.2.0",
    "mcp[cli]>=1.3.0",
    "cairosvg>=2.9.0",
    "garminconnect>=0.3.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "anyio>=4.0.0",
]

[project.scripts]
claude2strava-web = "claude2strava.web.app:main"
claude2strava-mcp = "claude2strava.mcp_server.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/claude2strava"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.hatch.metadata]
allow-direct-references = true
